@model SharedVm.ProductVm
@section JavaScript
{
    <script type="text/javascript" src="@Url.Content("~/js/productDetail.js")"></script>
}

<div class="container">
    <div class="row">
        <h2>@Model.Name</h2>
    </div>
    <hr />
    <br />
    <div class="row">
        <div class="col-sm">
            <div id="carouselExampleControls" class="carousel slide h-100" data-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="@Model.ImageLocation[0]" style="width:100px" alt="First slide">
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" src="@Model.ImageLocation[0]" style="width:100px" alt="Second slide">
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" src="@Model.ImageLocation[0]" style="width:100px" alt="Third slide">
                    </div>
                </div>
                <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>

        </div>
        <div class="col-sm">
            <ul class="list-group">
                <li class="list-group-item disabled"><h4>@Model.Description</h4></li>
                <li class="list-group-item">@Model.BrandId</li>
                <li class="list-group-item">@Model.Price</li>

                @{
//if (User.Identity.IsAuthenticated)
//{
                        <form action="" method="post">
                            <li class="list-group-item">
                                <button type="button" class="btn btn-warning">Mua Ngay</button>
                                <button type="button" class="btn btn-success" asp-controller="Cart" asp-action="AddsCart" asp-route-id="@Model.Id">Thêm Vào Giỏ Hàng</button>
                            </li>
                            </form>
                            @*}
                            else
                            {

                            <form action="" method="post">
                                <li class="list-group-item">
                                    <div class="input-group">
                                        <span class="input-group-btn">
                                            <button type="button" class="btn btn-light" onclick="Minus()" style="font-size:large">
                                                -
                                            </button>
                                        </span>
                                        <input type="number" id="quantity" name="quantity" style="width:50px" value="1" min="1" max="100">
                                        <span class="input-group-btn">
                                            <button type="button" class="btn btn-light" onclick="Plus()" style="font-size:large">
                                                +
                                            </button>
                                        </span>
                                    </div><br />
                                    <button type="submit" asp-route-id="@Model.Id" class="btn btn-success" asp-controller="Product" asp-action="AddsSession">Thêm Vào Giỏ Hàng</button>
                                </li>
                            </form>
                            }*@
                            }

                            @*<form action="" method="post">
                        <li class="list-group-item">
                            <div class="input-group">
                                <span class="input-group-btn">
                                    <button type="button" class="btn btn-light" onclick="Minus()" style="font-size:large">
                                        -
                                    </button>
                                </span>
                                <input type="number" id="quantity" name="quantity" style="width:50px" value="1" min="1" max="100">
                                <span class="input-group-btn">
                                    <button type="button" class="btn btn-light" onclick="Plus()" style="font-size:large">
                                        +
                                    </button>
                                </span>
                            </div><br />
                            <button type="submit" asp-route-id="@Model.Id" class="btn btn-success" asp-controller="Product" asp-action="AddsSession">Thêm Vào Giỏ Hàng</button>
                        </li>
                    </form>*@
                            <li class="list-group-item">
                                @using (Html.BeginForm("CreateRate", "Rate"))
                                {
                                    @Html.HiddenFor(x => x.Id)
                                    <select id="Star" name="Star">
                                        <option selected value="1">1</option>
                                        <option value="2">2</option>
                                        <option value="3">3</option>
                                        <option value="4">4</option>
                                        <option value="5">5</option>
                                    </select>
                                    <button type="submit" class="badge badge-warning" style="width:100px; color:white">Đánh giá</button>
                                }
                            </li>

                    </ul>
        </div>

    </div>
</div>
